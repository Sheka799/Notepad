class DASHBOARD {
    private root = ''

    HOME = this.root
    NOTEPADS = `${this.root}/notepads`
    SETTINGS = `${this.root}/settings`
    CREATE = `${this.root}/create`
}

export const DASHBOARD_PAGES = new DASHBOARD()